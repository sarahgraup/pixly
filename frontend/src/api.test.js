import PixlyApi from "./api"

// test("it gets all images", async function (){
//   const imageUrls = await PixlyApi.getImagesUrlsOptionalSearch();
//   console.log("imageUrls=", imageUrls);
//   expect(imageUrls).toBeInstanceOf("array");
// })

// test("it gets correct images with search", async function (){
//   const imageUrls = await PixlyApi.getImagesUrlsOptionalSearch("caption");
//   console.log("imageUrls=", imageUrls);
//   expect(imageUrls).toBeInstanceOf("array");
//   //TODO: check length
// })